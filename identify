function identified_im = identify(im) %will need to change 'im' and 'filtim' based on varname preferences
%input media file should only be a 1 variable input, but change if needed
SE = strel(im) %not sure if needed prior to the imdilate function
dilated_im = imdilate(im, SE); %strel vs offsetstrel
eroded_im = imerode(dilated_im, SE); %reverses dilation of im, helps to create contrast
for i=1:size(eroded_im,1); %will cycle through rows
    for k=1:size(eroded_im,2); %will cycle through columns
        if eroded_im(i,k)>= ## %insert threshold intensity value for best suited filtering
            %inensity values above the threshold will be delted, low
            %intensity (white) will remain
            filtered_im = bwareaopen(eroded_im); %will delte pixels that are below this value
        end
    end
end
end

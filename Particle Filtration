function zfishF = part_fil(zfishI)
first_ref = imread(zfishI, 1);
last_ref = imread(zfishI, numel(zfishI));
for i = 1:numel(zfishi)
 for r = size(zfishI, 2)
  for c = size(zfishI, 1)
    
   if 
   
   for 
   zfishF(r,c) = new_pixel; % sets each to position to its pixel
end
end
 
